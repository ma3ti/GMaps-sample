import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.lam2023.CellularConnectivityAndNoiseMap',
  appName: 'CellularConnectivityAndNoiseMap',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
